datasource db {
  provider = "sqlite"
  url      = "file:./dev/db"
}

generator client {
  provider = "prisma-client-js"
}

model User {
  id         String @id @default(uuid())
  login      String
  github_id  Int
  name       String
  avatar_url String

  @@map("users")
}
